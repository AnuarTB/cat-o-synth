class BeatQueue {
    constructor(beats) {
        this.beats = beats;
    }

    head() { 
        // [int, int, array]
        // return [bar, subbeat, array]
    }

    next() {
        // iterator++;
    }
}